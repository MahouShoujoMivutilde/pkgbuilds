pkgname=fake-dmenu
_pkgname=dmenu
pkgver=1
pkgrel=2
pkgdesc='Dummy package for everything that requires dmenu'
arch=('any')
license=('MIT')

provides=("$_pkgname")
conflicts=("$_pkgname")

package() {
    echo "Don't forget to symlink dmenu to something equivalent"
}
